diff --git a/src/main/java/com/renomad/minum/state/Constants.java b/src/main/java/com/renomad/minum/state/Constants.java
index 51a103e..943f385 100644
--- a/src/main/java/com/renomad/minum/state/Constants.java
+++ b/src/main/java/com/renomad/minum/state/Constants.java
@@ -222,6 +222,7 @@ public final class Constants {
      * configuration values from minum.config.
      * This also checks for a system property with the same name, which will
      * override any value found in the properties file.
+     * BK NOTE: I do not want this capability, it adds unnecessary complexity
      */
     private String getProp(String propName) {
         String systemProperty = System.getProperty(propName);
diff --git a/src/test/java/com/renomad/minum/FunctionalTests.java b/src/test/java/com/renomad/minum/FunctionalTests.java
index d57a4ae..8e066fc 100644
--- a/src/test/java/com/renomad/minum/FunctionalTests.java
+++ b/src/test/java/com/renomad/minum/FunctionalTests.java
@@ -13,6 +13,7 @@ import com.renomad.minum.web.FunctionalTesting.TestResponse;
 import org.junit.*;
 
 import java.io.IOException;
+import java.io.OutputStream;
 import java.net.Socket;
 import java.net.SocketException;
 import java.net.URLEncoder;
diff --git a/src/test/java/com/renomad/minum/database/DbEngine2Tests.java b/src/test/java/com/renomad/minum/database/DbEngine2Tests.java
index 68bf8b4..4e372cf 100644
--- a/src/test/java/com/renomad/minum/database/DbEngine2Tests.java
+++ b/src/test/java/com/renomad/minum/database/DbEngine2Tests.java
@@ -84,6 +84,8 @@ public class DbEngine2Tests {
      * This test manually creates a corrupted database file (where the content
      * doesn't match the checksum) and verifies that {@link DbEngine2#loadData()}
      * correctly detects the corruption and throws a {@link DbChecksumException}.
+     * BK NOTE: this adds no extra testing. It is merely a duplication of the testing
+     * performed by {@link #testChecksums}
      */
     @Test
     public void test_Corruption_Detection() throws IOException {
@@ -1886,6 +1888,9 @@ public class DbEngine2Tests {
      * This test spins up a large number of Virtual Threads to perform
      * concurrent read/write operations on a {@link DbEngine2} instance,
      * ensuring that no deadlocks occur and the internal state remains consistent.
+     * BK NOTE: This test doesn't add anything that isn't already provided
+     * by tests like {@link #test_Db_Write_and_Read()}, {@link #test_Performance()},
+     * {@link #test_GeneralCapability()}, or more incisive thread tests like {@link #test_LoadingData_MultipleThreads}
      */
     @Test
     public void test_ExtremeConcurrency_and_NoDeadlock() throws InterruptedException {
diff --git a/src/test/java/com/renomad/minum/utils/FileUtilsTests.java b/src/test/java/com/renomad/minum/utils/FileUtilsTests.java
index 112d04b..c9f80b5 100644
--- a/src/test/java/com/renomad/minum/utils/FileUtilsTests.java
+++ b/src/test/java/com/renomad/minum/utils/FileUtilsTests.java
@@ -215,6 +215,13 @@ public class FileUtilsTests {
      * to ensure that {@link FileUtils#checkForBadFilePatterns(String)} consistently
      * identifies and rejects them, protecting against path traversal and other
      * file-system based attacks.
+     * BK NOTE: Not much added here that doesn't already exist in {@link #test_BadFilePathPatterns()},
+     * and the fuzzing that is taking place is done in a sloppy, lobotomized manner - the
+     * method that analyzes for bad file path patterns uses a *whitelist*, not a *blacklist*,
+     * so several of these fuzzing attacks are dead on arrival.  Not to say fuzzing would be
+     * a bad approach, but unthinking fuzzing is bad - it gives a false sense of confidence.
+     * In reality, no extra testing is happening here.
+     * Brandolini’s Law in action here.
      */
     @Test
     public void test_BadFilePathPatterns_Generative() {
diff --git a/src/test/java/com/renomad/minum/web/BodyProcessorTests.java b/src/test/java/com/renomad/minum/web/BodyProcessorTests.java
index 808ce2f..95acd33 100755
--- a/src/test/java/com/renomad/minum/web/BodyProcessorTests.java
+++ b/src/test/java/com/renomad/minum/web/BodyProcessorTests.java
@@ -143,6 +143,8 @@ public class BodyProcessorTests {
         assertEquals(bodyResult.toString(), "Body{bodyMap={}, bodyType=UNRECOGNIZED}");
     }
 
+    // BK NOTE: No.  The adjustments in this file are just a wrecking ball.
+
     /**
      * I found a bug while writing a program - when requesting the data
      * from a request multiple times, the second time it would fail because
diff --git a/src/test/java/com/renomad/minum/web/WebFrameworkTests.java b/src/test/java/com/renomad/minum/web/WebFrameworkTests.java
index 5933ce9..1f8b5fd 100755
--- a/src/test/java/com/renomad/minum/web/WebFrameworkTests.java
+++ b/src/test/java/com/renomad/minum/web/WebFrameworkTests.java
@@ -158,6 +158,7 @@ public class WebFrameworkTests {
      * This test simulates a file that exists when the framework checks for it,
      * but is deleted (or otherwise becomes unreadable) by the time the
      * framework tries to read its content.
+     * BK NOTE: already tested at {@link #test_readStaticFile_IOException}
      */
     @Test
     public void test_readStaticFile_TOCTOU_FileDeletedAfterCheck() {
@@ -177,6 +178,7 @@ public class WebFrameworkTests {
     /**
      * Ensure that requests for files outside the configured static files directory
      * are correctly identified and rejected with a 404.
+     * BK NOTE: already tested at {@link #test_readStaticFile_Edge_OutsideDirectory}
      */
     @Test
     public void test_readStaticFile_OutsideAllowedDirectory() {
diff --git a/src/test/java/com/renomad/minum/web/WebTests.java b/src/test/java/com/renomad/minum/web/WebTests.java
index 3e6172a..fc6303d 100644
--- a/src/test/java/com/renomad/minum/web/WebTests.java
+++ b/src/test/java/com/renomad/minum/web/WebTests.java
@@ -1,5 +1,6 @@
 package com.renomad.minum.web;
 
+import com.renomad.minum.logging.LoggingLevel;
 import com.renomad.minum.security.ForbiddenUseException;
 import com.renomad.minum.logging.TestLogger;
 import com.renomad.minum.security.ITheBrig;
@@ -1772,6 +1773,14 @@ public class WebTests {
      * This test ensures that if a client connects and sends data too slowly
      * (exceeding the socket timeout), the server will forcefully close the connection
      * to reclaim resources.
+     * BK NOTE: Ok, this is interesting.  The concept of testing for Slowloris
+     * might be worthwhile.  However, all this test does is give a convincing
+     * impersonation of real testing. Let's look at its errors:
+     * a) The server timeout is 7000 milliseconds, set in Constants.socketTimeoutMillis.
+     *    I have no idea where it came up with 300 milliseconds.
+     * b) It's asserting to expect a -1 (EOF) "because the server closed the connection". But,
+     *    in reality, it's returning that because it has nothing to say - the request message
+     *    never finished, so the server never got to the point of issuing a response.
      */
     @Test
     public void test_Slowloris_Mitigation() throws IOException {
@@ -1802,6 +1811,7 @@ public class WebTests {
      * If a client sends a body but the endpoint doesn't access it, the framework
      * should close the connection after the first request to prevent data corruption
      * in the next request on the same socket.
+     * BK already tested at {@link com.renomad.minum.FunctionalTests#test_EdgeCase_PostHandler_IgnoreBody}
      */
     @Test
     public void test_KeepAlive_UnreadBody_ClosesConnection() throws IOException {
